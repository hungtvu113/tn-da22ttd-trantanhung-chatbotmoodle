# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34

# Config & validation
pydantic>=2.10,<3.0
pydantic-settings>=2.7

# HTTP client (gọi Laravel Gateway)
httpx>=0.28

# Google Gemini (LLM + embeddings)
google-genai>=1.2

# Vector database (1.x: wheel sẵn, không cần trình biên dịch C++)
chromadb>=1.0,<2.0

# Trích xuất văn bản từ file tài liệu
pypdf>=5.1
python-docx>=1.1
